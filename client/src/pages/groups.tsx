import { Hero } from "@/components/Hero";
import DefaultLayout from "@/layouts/default";

const Groups = () => {
  return (
    <DefaultLayout>
      <Hero />
    </DefaultLayout>
  );
};

export default Groups;
